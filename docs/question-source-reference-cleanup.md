# Batch 3 — source-reference stem audit

The original DOCX files in `MOQ/` were checked against importer manifest filenames, SHA-256 checksums, source sequences, and all 2,472 Production persisted stems before any change. The 171 broad discovery candidates resolved to 154 confirmed exam/source metadata annotations, 16 legitimate question-content matches, and one ambiguous inline suffix. The complete evidence for each candidate, including original source paragraphs and runs, remains in the ignored `.local-qa/batch3/classified.json`; all 154 exact identities, before/after text, original rich-text form, source filenames/checksums, and approved replacement rich-text form are in `scripts/question-reference-cleanups.json`.

The original Immunology DOCX question-cell text for MICRO 7 Q147 has a separate second paragraph `131/2020`; the authored question sentence is the first paragraph. MICRO 7 Q145 likewise has a separate second paragraph `131/2024`. Those values are previous-exam/source codes, not medical answers. The original Pathology and Biochemistry sources contain similar dedicated reference paragraphs, isolated reference runs, explicit “Previous paper”/“previous QP” suffixes, and parenthetical PSC exam labels. The established parser preserved these source annotations correctly but the import projection included them in `prompt` and `prompt_rich`. The import projection now uses a source-specific allowlist with exact deterministic ID, DOCX checksum, source occurrence, old text, and old rich text; it rejects any mismatched source instead of applying a general number/year rule. The original parser input and ID derivation remain unchanged.

The one unresolved case is MICRO 7 Q162, `5e97be4e-44bb-50f2-a7d0-0b8e492aee6e`: `162. The most abundant immunoglobulin is ____________ 069/2023`. Its code is inline within the same authored paragraph and run, without a distinct exam label or structural boundary. It stays unchanged pending client source review. Medical “reference”/“exam” uses and other benign discovery hits stay unchanged. No deduplication or medical/grammar edits were attempted.

QA and Production each changed exactly 154 existing question rows: Pathology 103, Microbiology 48, Biochemistry 3. Only the two display-stem columns (`prompt`, `prompt_rich`) changed; 2,472 canonical IDs and the 2,460 Active / 12 Draft distribution are intact. Full protected-table hashes and a questions projection excluding those two columns match their before-state in both databases. Options, answer keys, explanations, media, tests, recorded classes, programs, enrollments, source metadata, and existing attempt snapshots match. The guarded fix is idempotent (`production dry-run: 0 pending, 154 already_done`).

The local audit and executable guarded rollback artifacts are `.local-qa/batch3/production-rollback.json` and `.local-qa/batch3/production-rollback.sql`; equivalent QA artifacts are also retained. Each backup row contains the original imported stem, replacement stem, full rich-text forms, source identity/checksum, and reason. The DOCX bytes remain the recoverable source of truth. Neither rollback script has been run.

QA browser coverage at 1440px and 390×844 exercised Admin and Teacher list/search/detail, source-reference search, Manual and Random test-builder previews, and a disposable Student test with answer save, refresh, submission, and historical review. The disposable QA fixture was deleted after testing; final protected QA hashes match the post-fix baseline. Production browser coverage confirmed Admin/Teacher list/search/detail and all four `student@als.com` demo exams. `pnpm typecheck`, `pnpm lint`, `pnpm test` (214 Vitest and four importer tests), `pnpm build`, and 22 Python parser/fidelity tests passed.
