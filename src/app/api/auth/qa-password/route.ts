import{NextResponse}from"next/server";import{createClient}from"@/lib/supabase/server";
const enabled=()=>process.env.ENABLE_QA_PASSWORD_LOGIN==="true"&&process.env.VERCEL_ENV!=="production";
export function GET(){return NextResponse.json({enabled:enabled()})}
export async function POST(request:Request){if(!enabled())return NextResponse.json({error:"QA password login is disabled."},{status:404});let body:{email?:string;password?:string};try{body=await request.json()}catch{return NextResponse.json({error:"Invalid request."},{status:400})}if(!body.email||!body.password)return NextResponse.json({error:"Email and password are required."},{status:400});const db=await createClient();const{data,error}=await db.auth.signInWithPassword({email:body.email,password:body.password});if(error||!data.user)return NextResponse.json({error:"Invalid QA credentials."},{status:401});const{data:profile}=await db.from("profiles").select("role,is_active").eq("id",data.user.id).single();if(!profile?.is_active){await db.auth.signOut();return NextResponse.json({error:"Account is inactive."},{status:403})}return NextResponse.json({redirect:`/${profile.role}`})}
