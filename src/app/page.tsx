import { ArrowRight, BookOpen, ClipboardCheck, PlayCircle } from "lucide-react";
import { PublicHeader } from "@/components/shared/public-header";
import { ALSLogo } from "@/components/shared/als-logo";
import { Button } from "@/components/ui/button";

const subjects = ["Pathology", "Microbiology", "Biochemistry"];

export default function Home() {
  return <div className="min-h-screen bg-white"><PublicHeader/><main><section id="about" className="bg-surface py-20 sm:py-28"><div className="container-page grid items-center gap-12 lg:grid-cols-2"><div><p className="eyebrow">Academy for Laboratory Science</p><h1 className="mt-5 text-4xl font-extrabold leading-tight tracking-tight sm:text-6xl">Prepare for laboratory science assessments with ALS.</h1><p className="mt-6 max-w-xl text-lg leading-7 text-muted">Study assigned subjects, watch recorded classes and practice with timed assessments in one learning workspace.</p><div className="mt-8 flex flex-wrap gap-3"><Button href="/login">Sign in<ArrowRight size={17}/></Button><Button href="#courses" variant="secondary">Explore learning areas</Button></div></div><div className="rounded-2xl border border-line bg-white p-6 shadow-sm"><p className="text-sm font-bold uppercase tracking-wider text-brand">Your learning workspace</p><div className="mt-6 grid gap-4">{[[BookOpen,"Assigned courses","Follow the subjects included in your program."],[PlayCircle,"Recorded classes","Watch the lessons available to your enrollment."],[ClipboardCheck,"Exams & quizzes","Practice and review your results when an assessment is published."]].map(([Icon,title,body]) => {const Symbol=Icon as typeof BookOpen;return <div key={title as string} className="flex gap-4 rounded-xl bg-surface p-4"><Symbol className="shrink-0 text-brand" size={22}/><div><h2 className="font-bold">{title as string}</h2><p className="mt-1 text-sm text-muted">{body as string}</p></div></div>})}</div></div></div></section><section id="courses" className="container-page py-16"><p className="eyebrow">Academic subjects</p><h2 className="section-title mt-3">Learn by subject.</h2><p className="mt-3 max-w-2xl text-muted">These subjects are available through eligible ALS program enrollments. Your courses and assessments depend on your assigned access.</p><div className="mt-8 grid gap-4 sm:grid-cols-3">{subjects.map(subject => <article key={subject} className="card p-6"><BookOpen className="text-brand"/><h3 className="mt-4 text-xl font-bold">{subject}</h3><p className="mt-2 text-sm text-muted">Explore recorded classes and questions where published.</p></article>)}</div></section><section id="how-it-works" className="bg-deep-blue py-16 text-white"><div className="container-page"><h2 className="section-title">Learn. Practice. Review.</h2><p className="mt-4 max-w-2xl leading-7 text-blue-100">Sign in with your ALS account to access the subjects and assessments assigned to you. New learning resources appear as they are published.</p><Button href="/login" className="mt-7 bg-white! text-deep-blue!">Open your learning workspace</Button></div></section></main><footer id="contact" className="border-t border-line bg-surface"><div className="container-page flex flex-col gap-4 py-8 sm:flex-row sm:items-center sm:justify-between"><ALSLogo/><p className="text-sm text-muted">Academy for Laboratory Science</p><Button href="/login" variant="secondary">Account sign in</Button></div></footer></div>;
}
