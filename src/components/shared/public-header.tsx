"use client";import{Menu,X}from"lucide-react";import Link from"next/link";import{useState}from"react";import{ALSLogo}from"./als-logo";import{Button}from"@/components/ui/button";
const links=[{label:"Home",href:"/"},{label:"Courses",href:"/#courses"},{label:"About",href:"/#about"},{label:"How It Works",href:"/#how-it-works"},{label:"Contact",href:"/#contact"}];
export function PublicHeader(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-line/80 bg-white/90 backdrop-blur-xl"><div className="container-page flex h-20 items-center justify-between"><ALSLogo/><nav className="hidden items-center gap-7 lg:flex" aria-label="Main navigation">{links.map(x=><Link key={x.label} href={x.href} className="text-sm font-semibold text-muted transition hover:text-brand">{x.label}</Link>)}</nav><div className="hidden items-center gap-3 sm:flex"><Button href="/login" variant="ghost">Sign In</Button><Button href="/#courses">Explore Courses</Button></div><button className="grid h-11 w-11 place-items-center rounded-xl border border-line lg:hidden" onClick={()=>setOpen(!open)} aria-label="Toggle navigation" aria-expanded={open}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="container-page grid gap-2 border-t border-line py-4 lg:hidden">{links.map(x=><Link key={x.label} href={x.href} onClick={()=>setOpen(false)} className="rounded-lg px-3 py-3 text-sm font-semibold hover:bg-surface">{x.label}</Link>)}<Button href="/login" className="mt-2">Sign In</Button></nav>}</header>}
