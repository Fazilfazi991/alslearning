import{BookOpen,CalendarDays,FileQuestion,Users}from"lucide-react";import{TeacherShell}from"./teacher-shell";import{getTeacherData}from"@/lib/teacher-data";
export async function TeacherBackendPortal({section="dashboard"}:{section?:string}){const data=await getTeacherData();if(!data)return null;const title=({courses:"My Programs",students:"Students","live-classes":"Live Classes",assessments:"Tests","question-bank":"Question Bank"}as Record<string,string>)[section]||"Teacher Dashboard";return <TeacherShell title={title}><header className="mb-6"><p className="text-xs font-bold uppercase tracking-wide text-brand">Assignment-scoped workspace</p><h2 className="mt-2 text-2xl font-bold">{title}</h2><p className="mt-2 text-sm text-muted">Signed in as {data.user.full_name||data.user.email}</p></header>{section==="dashboard"&&<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={BookOpen} value={data.assignments.length} label="Assignments"/><Metric icon={Users} value={data.students.length} label="Visible students"/><Metric icon={CalendarDays} value={data.sessions.length} label="Sessions"/><Metric icon={FileQuestion} value={data.questions.length} label="Questions"/></div>}{section==="courses"&&<List rows={data.assignments.map(x=>({id:x.id,title:x.programs?.name||x.entrance_exams?.name||"Assignment",detail:x.subjects?.name||"All assigned subjects"}))} empty="No programs or subjects are assigned."/>}{section==="students"&&<List rows={data.students.map(x=>({id:x.id,title:x.profiles?.full_name||x.profiles?.email||"Student",detail:`${x.programs?.name||"Program"} · ${x.status}`}))} empty="No students are visible through your assignments."/>}{section==="live-classes"&&<List rows={data.sessions.map(x=>({id:x.id,title:x.title,detail:`${x.starts_at?new Date(x.starts_at).toLocaleString():"Not scheduled"} · ${x.provider_room_id?"Room ready":"Provider room unavailable"}`}))} empty="No live classes are scheduled."/>}{section==="assessments"&&<List rows={data.tests.map(x=>({id:x.id,title:x.title,detail:`${x.type} · ${x.question_count} questions · ${x.status}`}))} empty="No tests are assigned or created."/>}{section==="question-bank"&&<List rows={data.questions.map(x=>({id:x.id,title:x.prompt,detail:`${x.type} · ${x.subjects?.name||"Uncategorized"} · ${x.status}`}))} empty="No questions are available for your assignments."/>}{!["dashboard","courses","students","live-classes","assessments","question-bank"].includes(section)&&<List rows={[]} empty="No backend records are available in this area yet."/>}</TeacherShell>}
function Metric({icon:Icon,value,label}:{icon:typeof BookOpen;value:number;label:string}){return <div className="rounded-lg border border-[#e6cbd5] bg-white p-5"><Icon className="text-brand"/><strong className="mt-3 block text-3xl">{value}</strong><p className="text-sm text-muted">{label}</p></div>}
function List({rows,empty}:{rows:{id:string;title:string;detail:string}[];empty:string}){return rows.length?<div className="space-y-3">{rows.map(x=><article className="rounded-lg border border-[#e6cbd5] bg-white p-5" key={x.id}><h3 className="font-bold">{x.title}</h3><p className="mt-2 text-sm text-muted">{x.detail}</p></article>)}</div>:<section className="grid min-h-64 place-items-center rounded-lg border border-[#e6cbd5] bg-white p-8 text-center"><div><BookOpen className="mx-auto text-muted"/><h3 className="mt-4 font-bold">{empty}</h3><p className="mt-2 text-sm text-muted">Ask an administrator to configure the relevant assignment.</p></div></section>}
