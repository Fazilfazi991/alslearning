"use client";import{Search}from"lucide-react";import{useMemo,useState}from"react";import{CourseCard}from"@/components/course/course-card";import{EmptyState}from"@/components/ui/states";import{courses}from"@/lib/mock-data/courses";
const tabs=["All","In Progress","Completed"]as const;type Tab=typeof tabs[number];
export function CoursesBrowser(){const[tab,setTab]=useState<Tab>("All");const[query,setQuery]=useState("");const filtered=useMemo(()=>courses.filter(c=>(tab==="All"||tab==="Completed"&&c.status==="completed"||tab==="In Progress"&&c.status==="in-progress")&&c.title.toLowerCase().includes(query.toLowerCase())),[tab,query]);return <div className="mx-auto max-w-[1220px]"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="eyebrow">Your learning library</p><h2 className="mt-2 text-3xl font-extrabold tracking-tight">My Courses</h2><p className="mt-2 text-sm text-muted">Continue learning, revisit completed courses, and track your progress.</p></div><label className="relative w-full sm:max-w-xs"><span className="sr-only">Search my courses</span><Search className="absolute left-3 top-3 text-muted" size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search my courses" className="h-11 w-full rounded-xl border border-line bg-white pl-10 pr-3 text-sm"/></label></div><div className="mt-8 flex gap-2 border-b border-line" role="tablist">{tabs.map(x=><button key={x} role="tab" aria-selected={tab===x} onClick={()=>setTab(x)} className={`min-h-11 border-b-2 px-3 text-sm font-bold transition ${tab===x?"border-brand text-brand":"border-transparent text-muted hover:text-ink"}`}>{x}</button>)}</div>{filtered.length?<div className="mt-6 grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{filtered.map(c=><CourseCard key={c.id} course={c}/>)}</div>:<div className="mt-6"><EmptyState title="No courses found" message="Try another search or switch course filters."/></div>}</div>}
