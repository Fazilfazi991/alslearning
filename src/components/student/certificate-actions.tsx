"use client";import{useState}from"react";import{Button}from"@/components/ui/button";export function CertificateActions({label,icon}:{label:string;icon?:React.ReactNode}){const[msg,setMsg]=useState("");return <><Button variant="secondary" onClick={()=>{setMsg(`${label} is available in the connected-backend release.`);setTimeout(()=>setMsg(""),2200)}}>{icon}{label}</Button>{msg&&<div role="status" className="fixed bottom-24 right-5 z-50 rounded-xl bg-ink px-4 py-3 text-sm font-bold text-white shadow-xl">{msg}</div>}</>}
