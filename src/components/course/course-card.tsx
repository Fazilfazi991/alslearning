import{ArrowUpRight,Award,BookOpen,Clock}from"lucide-react";import Link from"next/link";import type{Course}from"@/types";import{ProgressBar}from"@/components/ui/progress";
export function CourseCard({course,publicView=false}:{course:Course;publicView?:boolean}){const href=publicView?"/login":course.slug==="clinical-biochemistry"?`/student/courses/${course.slug}`:"/student/courses";return <article className="card group flex h-full flex-col overflow-hidden p-5 transition hover:-translate-y-1 hover:shadow-[0_16px_38px_rgba(42,67,127,.12)]"><div className="mb-5 flex h-28 items-end rounded-xl bg-[linear-gradient(135deg,#fcecf5,#edf1fb)] p-4"><span className="rounded-full bg-white px-3 py-1 text-[11px] font-bold text-brand shadow-sm">{course.category}</span></div><h3 className="text-lg font-bold tracking-tight">{course.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-muted">{course.description}</p><div className="mt-4 flex flex-wrap gap-3 text-xs text-muted"><span className="flex items-center gap-1"><BookOpen size={14}/>{course.lessons} lessons</span><span className="flex items-center gap-1"><Clock size={14}/>{course.duration}</span></div>{!publicView&&<div className="mt-5"><ProgressBar value={course.progress}/></div>} {course.certificate&&<span className="mt-4 inline-flex items-center gap-1 text-xs font-bold text-[#8c681f]"><Award size={15}/>Certificate available</span>}<div className="mt-auto flex items-center justify-between border-t border-line pt-4 mt-5"><span className="text-xs text-muted">{course.instructor}</span><Link href={href} className="flex h-10 w-10 items-center justify-center rounded-full bg-surface text-brand transition group-hover:bg-brand group-hover:text-white" aria-label={`Open ${course.title}`}><ArrowUpRight size={18}/></Link></div></article>}
