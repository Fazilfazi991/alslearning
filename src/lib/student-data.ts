import "server-only";
import { createClient } from "@/lib/supabase/server";
import { currentUser } from "@/lib/auth";
export async function getStudentPortalData(){const user=await currentUser();if(!user)return null;const db=await createClient();const[enrollments,content,sessions,tests,progress,attempts]=await Promise.all([db.from("enrollments").select("*,programs(id,name,slug,description,duration_days),batches(id,name,starts_on,ends_on)").eq("student_id",user.id).order("created_at",{ascending:false}),db.from("learning_content").select("id,title,description,kind,slug,duration_seconds,program_id,subject_id,chapter_id,topic_id,external_url,allow_download").eq("status","active").order("display_order"),db.from("live_sessions").select("id,title,starts_at,ends_at,status,provider,provider_room_id,program_id,batch_id,subjects(name),topics(name),profiles!live_sessions_faculty_id_fkey(full_name)").in("status",["scheduled","live"]).order("starts_at"),db.from("tests").select("id,title,slug,type,duration_minutes,question_count,total_marks,available_from,available_until,program_id").eq("status","active").order("available_from"),db.from("video_progress").select("content_id,position_seconds,completed,updated_at").eq("student_id",user.id),db.from("test_attempts").select("id,test_id,started_at,submitted_at,score,status").eq("student_id",user.id).order("started_at",{ascending:false})]);const error=[enrollments,content,sessions,tests,progress,attempts].find(x=>x.error)?.error;if(error)throw new Error(error.message);return{user,enrollments:enrollments.data||[],content:content.data||[],sessions:sessions.data||[],tests:tests.data||[],progress:progress.data||[],attempts:attempts.data||[]}}
